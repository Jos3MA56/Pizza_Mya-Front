// src/store/auth.store.js
/**
 * DEPRECADO — este archivo usaba la key "session" en localStorage
 * mientras el resto del proyecto usa "pmya_token" y "pmya_user".
 *
 * Todo el estado de autenticación está en AuthContext.
 * Importa desde ahí:
 *
 *   import { useAuth } from "../context/AuthContext.jsx";
 *
 * Este archivo se mantiene vacío para no romper imports existentes.
 */

export {};
